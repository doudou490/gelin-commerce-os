services:
  - type: web
    name: gelin-commerce-os
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /api/health
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: lavea-db
          property: connectionString
      - key: PGSSL
        value: "true"

databases:
  - name: lavea-db
    databaseName: lavea_db
    plan: free
